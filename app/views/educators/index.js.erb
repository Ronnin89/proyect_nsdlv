$('#card_educators').empty()

<% @educators.each do |educator|%>
    $('#card_educators').append(`
                <div class="card mb-3" style="max-width: 540px;">
                    <div class="row no-gutters">
                        <div class="col-md-4">
                        <img src="" alt="niñe">
                        </div>
                        <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">Nombre: <%= educator.names %> <%= educator.last_names %></h5><hr>
                            <p class="card-text">RUT: <%= educator.rut %></p><hr>
                            <p class="card-text">Fecha de nacimiento: <%= educator.date_of_birth %></p><hr>
                            <button type="button" class="btn btn-primary"><%= link_to 'Ver mas', educator, remote: true %></button>
                        </div>
                        </div>
                    </div>
                </div>   
                `)
<% end%>